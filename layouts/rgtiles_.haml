.row
  - $rgs.values.sort.each do |rg|
    .col-lg-2.col-md-3.col-sm-4.col-xs-6
      %a{ :href => "https://datatracker.ietf.org/rg/" + rg.acronym.downcase + "/about/" }
        .thumbnail.rg
          .text-center
            %b=rg.acronym.upcase
          .text-center
            %small
              %em=rg.name
