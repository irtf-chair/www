---
is_hidden: true
---

- year = item.path.split("/")[2]

!!! 5
%html{ :lang => "en" }
  = render "/head_.*", :loc => "../../"

  %body
    %nav.navbar.navbar-default
      .container-fluid
        %h1 <small>IRTF & Internet Society, in Cooperation with ACM SIGCOMM</small> <br>Applied Networking Research Workshop #{year}
        .navbar-header
          %a.navbar-brand{ :href => "../../anrw" } ANRW Series
          %button.navbar-toggle.collapsed{ :type => "button", "data-toggle" => "collapse", "data-target" => "#anrw-collapse", "aria-expanded" => "false" }
            %span.sr-only Toggle navigation
            %span.icon-bar
            %span.icon-bar
            %span.icon-bar
        .collapse.navbar-collapse#anrw-collapse
          %ul.nav.navbar-nav
            - (@items.find_all(%r{anrw/#{year}}).sort_by do |item| item.path ? item.path : "" end).each do |item|
              - if (item.path =~ %r{(/|html)$})
                %li{ :class => ("active" if item[:menu] == @item[:menu])}
                  %a{ :href => "." + item.path.split(year)[-1] } #{item[:menu]}

    .container-fluid
      = yield

    = render "/footer_.*", :loc => "../../"
    = render "/scripts_.*", :loc => "../../"
